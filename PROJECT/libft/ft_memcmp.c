// memcmp
#include "libft.h"

int memcmp(const void *s1, const void *s2, size_t n)
{
    int index = 0;

    while (index<n)
    {
        ((int*)s1[]);
        ((int*)s2[]);

        if (s1[index]<s2[index])
        {
            /* code */
        }
        if ()
        else
        {

        }
        ++index
    }
    
}