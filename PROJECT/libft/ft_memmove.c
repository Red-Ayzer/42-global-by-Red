// memmove
#include "libft.h"

void *ft_memmove(void *dest, const void *src, size_t n)
{
    int index = 0;
    (int*)area[index]

    while (index < n)
    {
        ((int*)src[index]) = area[index]
        area[index] = ((int*)dest[index])

        index ++
    }
    return ((int*)dest)
}