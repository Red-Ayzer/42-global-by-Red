// memchr
#include "libft.h"

void *ft_memchr(const void *s, int c, size_t n)
{
    int s = 0;
    unsigned char c;

    while (s<n)
    {
        ((int *)memory[]);

        if (memory[s]=c)
        {
            return memory[]
        }
        else
        {
            ++s
            return NULL
        }
    }
    
}